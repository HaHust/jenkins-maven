pipeline {
    agent any
    tools { 
      maven 'jenkins-maven'
      docker 'docker'
    }
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/HaHust/jenkins-maven'
            }
        }
        
        stage('test') {
            steps {
                echo 'haha'
            }
        }
        
        stage('build docker') {
            steps {
                sh 'docker build -t ha/app .'
            }
        }
    }

}
